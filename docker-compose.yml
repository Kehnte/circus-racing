services:
  circus-racing:
    image: ghcr.io/kehnte/circus-racing-server:latest
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - circus-db:/app/db
    restart: unless-stopped

volumes:
  circus-db:
